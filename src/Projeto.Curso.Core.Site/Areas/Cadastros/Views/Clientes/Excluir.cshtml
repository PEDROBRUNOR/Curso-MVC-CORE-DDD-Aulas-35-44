@model Projeto.Curso.Core.Application.Pedido.ViewModels.ClientesViewModel

@{
    ViewData["Title"] = "Excluir";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row margem-top-10">
    <div class="col-md-12">
        <vc:summary />
        <div class="panel panel-danger">
            <div class="panel-heading">
                <i class="glyphicon glyphicon-trash"></i>
                <strong>CLIENTES - EXCLUSÃO - Você tem certeza que deseja excluir este cliente?</strong>
            </div>
            <div class="panel-body padding-25">
                <div class="row">
                    <form asp-action="Excluir" autocomplete="off">
                        <input asp-area="Cadastros" asp-for="Id" type="hidden">
                        @Html.Partial("_FormClientesDetalhe")

                        <div class="row">
                            <div class="col-md-2">
                                <button type="submit" class="btn btn-block btn-danger"><i class="glyphicon glyphicon-trash"></i> Excluir</button>
                            </div>

                            <div class="col-md-2">
                                <a asp-area="Cadastros" onclick="return LigaAguarde()" asp-controller="Clientes" asp-action="Index" class="btn  btn-block btn-default"><i class="glyphicon glyphicon-arrow-left"></i> Voltar</a></a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @if (@ViewBag.RetornoPost != "" && @ViewBag.RetornoPost != null)
    {
        var notificacao = ViewBag.RetornoPost.Split(',');
        <script>
            ExibeMensagem("@notificacao[0]", "@notificacao[1]");
        </script>
    }
}
